# Display student information using different data types.
START
Declare name as string
Declare age as integer
Declare grade as character
Declare marks as float

Input name
Input age
Input grade
Input marks

Display name
Display age
Display grade
Display marks
END
# Read and display a character using getchar() and putchar()
START
Declare ch as character

Read ch using getchar()
Display ch using putchar()

END
# Display a floating-point value using different precision settings
START
Declare marks as floating-point

Input marks

Display marks with 2 decimal places
Display marks with 3 decimal places
Display marks with 4 decimal places

END
